{
  "name": "sentinel-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "cd server && npm install",
    "build": "cd server && npx prisma generate && npm run build",
    "start": "cd server && node dist/index.js"
  }
}
