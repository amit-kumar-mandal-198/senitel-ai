{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -d 'server' ]; then cd server; fi && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "if [ -d 'server' ]; then cd server; fi && npx prisma db push --skip-generate || true && node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
