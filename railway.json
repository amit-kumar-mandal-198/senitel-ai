{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && npm install && npx prisma db push && npx ts-node prisma/seed.ts && npm run dev",
    "restartPolicyType": "ON_FAILURE"
  }
}
